rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /lms/lessonSchedule {
      allow read: if true;
      allow write: if request.auth != null
        && request.auth.token.email in [
          "prasadboyane@gmail.com"
        ];
    }
  }
}
